import { html } from './lib.js';
export function LazyTab() { return html`<section class="panel" id="panel-lazy"><div class="card empty">Ленивый крафтер — на следующем этапе переезда.</div></section>`; }
